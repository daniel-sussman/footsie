<div class="box-container-left">
  <div class="row ">
    <div class="col-md-12 mt-2 ">
      <div class="row">
        <% @games.each do |game| %>
          <div class="box col-md-3" style="color: <%= @open_games.include?(game) ? 'green' : 'red' %>;">
            <% if game.photo.present? %>
              <%= cl_image_tag game.photo.key, crop: :fill %>
            <% else %>
              <%= image_tag "football_#{%w[green red blue purple orange teal azul beige navy rojo sun verde yellow][game.id % 13]}.png" %>
            <% end %>
            <h2><%= game.name %></h2>
            <p>Kick off: <%= game.starting_date %></p>
            <p>£<%= game.price %> per person</p>
            <p>Gender: <%= game.gender %> only</p>
            <p>Address: <%= game.address %></p>
            <div class="d-flex justify-content-between mt-8">
              <button class="button2 mt-2"><span><%= link_to "Read more", game_path(game) %></span></button>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </div>
</div>
